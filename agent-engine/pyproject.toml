[project]
name = "agentflow-engine"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.141,<1", "uvicorn>=0.53,<1", "langgraph>=1.2,<2", "httpx>=0.28,<1", "sqlalchemy>=2.0,<3", "psycopg[binary]>=3.3,<4"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
